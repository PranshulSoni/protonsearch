- `[x]` Add `"Media_SpeechRecognition"` feature to the windows crate dependencies in `Cargo.toml`.
- `[x]` Implement continuous speech recognition loop and wake phrase detection in `src/voice.rs`.
- `[x]` Implement dictation transcription and voice command posting in `src/voice.rs`.
- `[x]` Add `voice_listening`, `voice_triggered`, and `voice_dot_tick` fields to `State` in `src/main.rs`.
- `[x]` Add `WM_VOICE_WAKEWORD` and `WM_VOICE_QUERY_READY` message handlers in `src/main.rs`.
- `[x]` Hook up timer events for `TIMER_VOICE_ANIM` (dot pulse) and `TIMER_VOICE_AUTOEXEC` (auto-execute).
- `[x]` Implement `execute_selected` helper function to launch selected results.
- `[x]` Cancel voice listening/auto-execute on keyboard typing, arrow navigation, scrolling, and mouse clicks.
- `[x]` Update the UI drawing in `paint` to show a pulsing red dot, "Listening..." placeholder, and green "▶ Executing..." badge.
- `[x]` Call `voice::start_wake_word_listener` at window creation.
- `[x]` Verify compilation and execution via cargo build release.
